// Include in this file any constant values that can/may be changed in the future

package main

// PORT is the port on which the server would be run
var PORT string = ":8000"

// Credentials to connect to Postgres database
// TODO remove these
const (
	HOST     = "db"
	DBPORT   = 5432
	USER     = "admin"
	PASSWORD = "password"
	DBNAME   = "db"
)
